fn main() {
    println!("Project Euler No.1 with optimization.");
    
    let limit = 1000;
    let sum: u32 = (1..limit)
        .filter(|&x| x % 3 == 0 || x % 5 == 0)
        .sum();

    println!("The sum of all multiples of 3 or 5 below {} is {}", limit, sum);
}
